# REFLECTION.md
## ขั้นตอนที่ A7 — บันทึกการทบทวนย้อนหลัง (Retrospective)

---

### 1. มีเทสต์ทั้งหมดกี่ตัว และโค้ดจริงกี่บรรทัด อัตราส่วนเป็นเท่าใด
* **จำนวนเทสต์ทั้งหมด:** 6 ฟังก์ชันการทดสอบ (`test_kitchen.py`)
  * `test_multiplication`
  * `test_multiplication_returns_new_object` (ตรวจสอบ immutability / value object)
  * `test_equality` (ทดสอบความเท่ากันทั้งกรณีค่าเท่าและหน่วยต่าง)
  * `test_simple_addition`
  * `test_addition_with_different_units`
  * `test_sum_multiplication`
* **จำนวนบรรทัดโค้ด (LOC):**
  * **Test Code (`test_kitchen.py`):** ประมาณ 40–50 บรรทัด (รวม factory functions `grams`, `ounces`)
  * **Production Code (`kitchen.py`):** ประมาณ 45–55 บรรทัด (ประกอบด้วยคลาส `Quantity`, `Sum`, และ `Converter`)
* **อัตราส่วน (Test : Production Code):** ประมาณ **1 : 1** (หรือใกล้เคียง 0.9 : 1)
  * **ข้อสังเกต:** อัตราส่วนที่ใกล้เคียงกันนี้สะท้อนหลักการของ TDD โดยตรง คือเราไม่ได้เขียนโค้ดจริงเกินความจำเป็นแม้แต่บรรทัดเดียว โค้ดทุกบรรทัดใน `kitchen.py` ล้วนถูกขับเคลื่อนขึ้นมาเพราะมีเทสต์ตัวใดตัวหนึ่งเรียกร้องให้ต้องเขียน

---

### 2. ช่วงที่แถบเป็นสีแดงนานที่สุดคือเมื่อใด และอะไรทำให้มันนาน
* **ช่วงเวลาที่แดงนานที่สุด:** เกิดขึ้นใน **ขั้นตอน A6 (การจัดการการบวกสองปริมาณและการแปลงหน่วยผ่าน `Converter`)**
* **สาเหตุ:**
  1. มีการเปลี่ยนกระบวนทัศน์จากการคำนวณค่าทันที (Eager Evaluation) ไปเป็นการสร้าง **Expression Object (`Sum`)** ที่ผัดผ่อนการคำนวณออกไป (Deferred Evaluation)
  2. เกิดความสับสนในขั้นตอนการกำหนด Signature ระหว่าง `Converter.reduce(source, to_unit)` กับ `source.reduce(to_unit, converter)` ว่าหน้าที่คำนวณควรอยู่ที่ใคร
  3. เทสต์ส่งผลให้เกิด `AttributeError` และ `TypeError` สลับกันหลายครั้งในรอบ Fake It -> Real implementation ส่งผลให้รอบวงจร Red–Green ใช้เวลานานกว่าขั้นตอนก่อนๆ ที่เป็นแค่การคูณหรือเปรียบเทียบค่าเดี่ยว

---

### 3. การเลือกใช้กลยุทธ์ทั้ง 3 รูปแบบ (Fake It, Triangulate, Obvious Implementation)

| กลยุทธ์ | จุดที่นำมาใช้ | การประเมินความเหมาะสมเมื่อมองย้อนกลับไป |
| :--- | :--- | :--- |
| **Fake It** | **ขั้นตอน A1 & ก้าวแรกของ A6:** <br>- ใน A1 ฮาร์ดโค้ด `return 600`<br>- ใน A6 ให้ `plus()` คืน `grams(500)` ทันที | **เหมาะสมมาก:** ช่วยยืนยันว่าโครงสร้างสายการเรียก (Plumbing / Wiring) ระหว่างเทสต์กับคลาสทำงานเชื่อมต่อกันได้จริงโดยไม่ต้องกังวลเรื่องลอจิก ทำให้เริ่มจากแถบเขียวก่อนขยับไปสู่ก้าวที่ใหญ่ขึ้น |
| **Triangulate** | **ขั้นตอน A2 (การคูณ):** <br>เขียน `test_multiplication_by_two` เพื่อบังคับให้เปลี่ยนจากการฮาร์ดโค้ด `600` ไปเป็นสูตรทั่วไป `self.amount * multiplier` | **เหมาะสมสำหรับผู้เริ่มต้น:** การมีสองตัวอย่างที่ขัดแย้งกันบีบให้เราต้องเขียนลอจิกที่ถูกต้อง แม้จะรู้สึกว่าช้า แต่ช่วยสร้างความมั่นใจและเห็นกลไกการขับเคลื่อนโค้ดชัดเจน |
| **Obvious Implementation** | **ขั้นตอน A4 & A5:** <br>- การเขียนเมท็อด `__repr__`<br>- การเพิ่มฟิลด์ `unit` ใน constructor และเทียบใน `__eq__` | **เหมาะสม:** เพราะเป็นลอจิกพื้นฐานที่ไม่ซับซ้อน ชัดเจนในตัวเอง การเขียนลงไปตรงๆ ช่วยประหยัดเวลาโดยไม่ก่อให้เกิดบั๊กไม่คาดคิด |

---

### 4. รายการทดสอบที่ยังค้างอยู่ รายการใดจะทำจริง และรายการใดควรลบทิ้ง
* **รายการที่คงไว้และจะลงมือทำจริง:**
  * `☐ รองรับการแปลงหน่วยแบบโซ่หรือทศนิยมละเอียดขึ้น (เช่น oz เป็น g ที่มีเศษทศนิยม)`: ควรทำต่อเพราะเป็น domain logic จริงของการทำอาหารที่มีความสำคัญต่อสูตร
  * `☐ การบวก Sum ซ้อน Sum เช่น (A + B) + C`: ควรทำเพื่อยืนยันว่า `Sum` ทำงานแบบ Composite Pattern ร่วมกับ `Quantity` ได้อย่างสมบูรณ์
* **รายการที่ควรลบทิ้ง (Speculative Generality / YAGNI):**
  * `☐ การนิยาม __hash__ สำหรับ Quantity`: ควรลบทิ้ง เนื่องจากในบริบทของแอปสูตรอาหารตอนนี้ ยังไม่มีความจำเป็นต้องนำ `Quantity` ไปใช้เป็นคีย์ของ `dict` หรือสมาชิกใน `set` การเขียนรองรับไว้ล่วงหน้าเป็นการคาดเดาความต้องการที่ยังไม่เกิดขึ้นจริง

---

### 5. เมท็อด `Quantity.reduce` หากออกแบบบนกระดาษก่อน จะเขียนออกมาหรือไม่
* **คำตอบ:** **แทบจะไม่มีทางเขียนออกมาในรูปแบบนี้**
* **เหตุผล:**
  * หากออกแบบบนกระดาษล่วงหน้า (Upfront Design) ความคิดแรกตามธรรมชาติของนักพัฒนาคือการนำความรับผิดชอบทั้งหมดไปใส่ไว้ที่ `Converter` เช่น การสร้างเมท็อด `converter.convert(amount, from_unit, to_unit)` แล้วให้ตัวคำนวณภายนอกเป็นผู้ดึงค่าตัวเลขออกมาแปลง
  * แต่เนื่องจากกระบวนการ TDD ในขั้นตอนการบวก ผลักดันให้เราต้องจัดการกับนิพจน์ซับซ้อนอย่าง `Sum` ทำให้เกิดแนวคิด **Polymorphism** ที่ทั้ง `Quantity` และ `Sum` มีเมท็อด `reduce` เหมือนกัน ส่งผลให้โค้ดมีความยืดหยุ่นสูง (Decoupled) และรองรับสูตรซ้อนสูตรได้อย่างสง่างาม ซึ่งเป็นการออกแบบที่เกิดจาก "เทสต์นำทาง" ไม่ใช่การจินตนาการโครงสร้างล่วงหน้า